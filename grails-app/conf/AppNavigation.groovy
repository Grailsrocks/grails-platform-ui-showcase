navigation = {
    app {
        home(controller:'content', action:'index')
        products(controller:'content')
        profile(controller:'content')
        signup(controller:'content')
        admin(controller:'content')
    }
}